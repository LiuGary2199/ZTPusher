using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WedCheck : MonoBehaviour
{
[UnityEngine.Serialization.FormerlySerializedAs("boxgroup")]    public List<GameObject> Solidify;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
